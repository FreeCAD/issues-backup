### FreeCAD issues backup

This repo contains an automatic backup of the issues, comments and pull requests made on the FreeCAD/FreeCAD repository, together with the script used to perform the backup.
